# PickPresence environment example

# Force placeholder clip export (skip ffmpeg) – useful for local tests only.
# PICKPRESENCE_FORCE_PLACEHOLDER=1

# Detector wrapper script invoked by pickpresence.detector_runner. Must be executable.
PICKPRESENCE_DETECTOR_SCRIPT=/home/zhibai/PickPresence/detectors/run_detector.sh

# Reference embedding JSON used by scripts/demo.sh / CLI (required for demo).
# Use scripts/make_reference.py to generate this file.
PICKPRESENCE_REFERENCE_EMBEDDING=/home/zhibai/PickPresence/out/refs/alice_ref_insightface.json

# 调试用的：它会在 out/dumps 里输出 12 张带框的图片，方便验收“男主镜头是否被错误匹配”
PICKPRESENCE_DETECTOR_ARGS=--dump-frames 12 --dump-dir /home/zhibai/PickPresence/out/dumps

# Optional: override the directory for generated artifacts/demo outputs.
PICKPRESENCE_OUTPUT_DIR=/home/zhibai/PickPresence/out

PICKPRESENCE_CLI_ARGS="--segment-policy track_first --match-threshold-start 0.55 --match-threshold-keep 0.2 --bridge-gap 1.2"
