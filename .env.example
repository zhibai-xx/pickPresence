# PickPresence environment example

# Force placeholder clip export (skip ffmpeg) – useful for local tests only.
# PICKPRESENCE_FORCE_PLACEHOLDER=1

# Detector wrapper script invoked by pickpresence.detector_runner. Must be executable.
# By default this points to detectors/run_detector.sh which launches .venv-detector's python
# against detectors/insightface_detector.py.
# PICKPRESENCE_DETECTOR_SCRIPT=/absolute/path/to/detectors/run_detector.sh

# Optional: override the python binary used for pickpresence.cli (demo + manual runs).
# If unset, scripts/demo.sh auto-detects .venv-detector/bin/python to keep dependencies aligned.
# CLI_PYTHON=/absolute/path/to/.venv-detector/bin/python

# IMPORTANT (M9 acceptance + audit):
# For video-trim + audit, use the detector venv and set PYTHONPATH to the repo.
# Example:
# PYTHONPATH=/abs/path/to/PickPresence /abs/path/to/PickPresence/.venv-detector/bin/python -m pickpresence.cli ...
# PYTHONPATH=/abs/path/to/PickPresence /abs/path/to/PickPresence/.venv-detector/bin/python scripts/audit_segments.py ...

# Reference embedding JSON used by scripts/demo.sh / CLI (required for demo).
# Use scripts/make_reference.py --backend insightface to generate this file.
# PICKPRESENCE_REFERENCE_EMBEDDING=/absolute/path/to/reference_target.json

# Optional: override the directory for generated artifacts/demo outputs.
# PICKPRESENCE_OUTPUT_DIR=/tmp/pickpresence_out

# Optional: pass custom CLI arguments for demo/quick tuning (quoted string, supports spaces).
# PICKPRESENCE_CLI_ARGS="--segment-policy track_first --match-threshold-start 0.55 --match-threshold-keep 0.2 --merge-policy union --trim-policy head_tail --trim-threshold-start 0.6 --trim-threshold-keep 0.3 --trim-pad 0.2 --min-duration 1.2 --bridge-gap 1.2"

# Multi-reference (M10) examples:
# PICKPRESENCE_CLI_ARGS="--reference-dir /abs/path/to/refs --reference-agg topk_avg --reference-topk 2 --segment-policy track_first --merge-policy union --trim-policy head_tail --trim-source video --trim-scan-window 0.6 --trim-scan-step 0.04 --export-end-eps 0.2"
